{
  "name": "blueprint.app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "live-server": "^1.2.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "live-server --port=3000 --open=index.html --watch=index.html"
  }
}